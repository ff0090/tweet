<div class="container">
  <div class="row">
    
    <div class="col-sm-4" id="tweet_text">
        <h2>Twitter風アプリ</h2>
        <p>このサイトはruby on railsの練習に作成されました。</p>
        <p>content一覧ページです</p>
        <p><%= notice %></p>
        <p><%= link_to "作成する", new_tweet_path %></p>
    </div>
    
    <div class="col-sm-8 tweet_box" >
    
      <% @tweets.each do |tweet| %>
        <div class="tweets">
          <p><%= tweet.content %></p>
          <%= link_to "tweetを編集する", edit_tweet_path(tweet.id), class: 'btn btn-default btn-sm btn-success' %>
          <%= link_to "削除", tweet_path(tweet.id), method: :delete, data: { confirm: '本当に削除していいですか？'}, class: 'btn btn-default btn-sm btn btn-danger' %>
        </div>
      <% end %>    
      
    </div>
  </div>
</div>